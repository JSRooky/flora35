import getLocationCursorUrl from "./images/cursor_get_location.svg";

/** Курсор указания места на карте; fallback — crosshair. */
export const GET_LOCATION_CURSOR = `url("${getLocationCursorUrl}") 16 16, crosshair`;
